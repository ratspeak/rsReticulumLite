# API fixture

`fixtures/` is a separate Cargo workspace that checks library construction and
outbound draining from a downstream crate. The test matrix compiles it.

Update the fixture when changing the public API.
